This folder will contain ffmpeg.wasm core files at runtime:
- ffmpeg-core.js
- ffmpeg-core.wasm
- ffmpeg-core.worker.js

On first load, the library will fetch these relative to /ffmpeg/. If missing, conversion will fail.
